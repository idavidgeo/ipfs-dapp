export default function Spinner({ className }) {
  return <div className={`grid-1 my-auto size-12 border-t-transparent border-solid animate-spin rounded-full border-white border-2 ${className}`}></div>;
}
